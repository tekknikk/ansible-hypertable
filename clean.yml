---
- name: Cleaning hypertable and DELETE ALL THE DATA
  hosts: masters
  user: hadoop
  sudo: false
  gather_facts: false
  vars:
     master: "{{ lookup('file', './panda3T_masters') }}"
  vars_files:
    - ./group_vars/build
  roles:
    - clean